# Example environment configuration for MCP SourceKit-LSP development
# Copy this file to .env and fill in your values

# Pushover credentials for development notifications (optional)
# Sign up at https://pushover.net to get these credentials
# Used by bin/notify script for development updates
PUSHOVER_TOKEN=your_pushover_app_token_here
PUSHOVER_USER=your_pushover_user_key_here

# SourceKit-LSP configuration (optional)
# Path to sourcekit-lsp binary - uncomment and set if not in PATH
# SOURCEKIT_LSP_PATH=/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/sourcekit-lsp

# Build arguments for sourcekit-lsp (optional)
# Useful for Xcode projects that need specific SDK or flags
# SOURCEKIT_BUILD_ARGS=-Xswiftc -sdk -Xswiftc /path/to/sdk